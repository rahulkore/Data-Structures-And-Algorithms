package DesignPatterns.Patterns.CommandPattern;

public class Command {

    /**
     * Command Pattern 
     * The command Pattern encapsulates a request as an object, thereby letting you parametrize 
     * other objects with different requests, queue or log requests, and support un doable operations. 
     * @param args
     */
    public static void main(String[] args) {
        
    }
    
}
